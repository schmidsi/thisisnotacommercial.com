frontend:
  build:
    context: ./frontend
    dockerfile: Dockerfile
controlpanel:
  build:
    context: ./controlpanel
    dockerfile: Dockerfile
unchained:
  build:
    context: ./unchained
    dockerfile: Dockerfile
